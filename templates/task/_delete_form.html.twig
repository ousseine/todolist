<form method="post" action="{{ path('app_task_delete', {'id': task.id}) }}" class="form-delete" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ task.id) }}">
    <button class="btn btn-sm p-0"><i class="bi bi-x-circle"></i></button>
</form>
